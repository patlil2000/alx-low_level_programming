#include "main.h"

/**
 * _abs -  computes the absolute value of an integer
 *
 * @i: first integer
 *
 * Return: return 0 if program is successfu;
 */
int _abs(int i)
{
	if (i >=  0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
