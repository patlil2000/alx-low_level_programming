#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 * Return: returns 0 when program is successful
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	return (0);
}
