#include "main.h"

/**
 * mul - multiplies two numbers
 *
 *@a: first number
 *
 *@b: second number
 *
 * Return: return 0 if successful
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
